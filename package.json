{
  "name": "fantaf12026",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "sync": "npx cap sync"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
