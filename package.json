{
  "name": "fantaf12026",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capgo/capacitor-updater": "^8.45.1",
    "@revenuecat/purchases-capacitor": "^12.1.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "sync": "npx cap sync",
    "release:set-build": "node scripts/bump-build-number.mjs",
    "ota:build": "npm --prefix web run build",
    "ota:upload": "node scripts/capgo-upload.mjs"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
